docker-machine create --driver virtualbox new
docker-machine ls

docker-machine env default

This line makes you appear as the docker and run commands:
eval "$(docker-machine env default)"

GET IP

docker-machine ip default

docker pull python
docker run python python --version



// Start / Stop Machine
docker-machine start default
docker-machine stop default


docker run -d -P selenium/standalone-chrome

docker run -d -P --name selenium-hub selenium/hub
docker run -d --link selenium-hub:hub selenium/node-chrome


docker run -d -p 32780:4444 -p 5999:5900 selenium/standalone-chrome

git clone https://github.com/Ekunga/Instagram.git

docker ps

docker ps -a

docker rm (then the number)

// Exits all that have exited
docker rm $(docker ps -a -q -f status=exited)

docker run -it busybox sh

docker ps -a (This will show container name)
docker port (Container Name)


cd ~

python instagram_hitter.py


python/3.6/windows/windowsservercore/Dockerfile